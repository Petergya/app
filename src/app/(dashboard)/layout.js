export default function DashboardLayout({children}){
    return(
        <main className="flex w-full">
            <div className="w-[30%]">
                
            </div>
            {children}
        </main>
    )
}